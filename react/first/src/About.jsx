import React from 'react'

const About = ({ userName, age }) => {
    return (
        <div>
            about page

            <h1>{userName} - {age}</h1>
        </div>
    )
}

export default About
